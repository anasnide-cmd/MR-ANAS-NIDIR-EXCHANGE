{
  "name": "manex",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^10.0.0",      // Kemas kini kepada versi terbaru Firebase
    "react": "^18.2.0",         // Kemas kini React ke versi terbaru
    "react-dom": "^18.2.0",     // Kemas kini ReactDOM ke versi terbaru
    "react-router-dom": "^6.4.0", // Kemas kini React Router ke versi terbaru
    "tailwindcss": "^3.0.0"     // Kemas kini TailwindCSS ke versi terbaru
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",  // Kemas kini Autoprefixer
    "postcss": "^8.4.5",        // Kemas kini PostCSS
    "react-scripts": "^5.0.0"    // Kemas kini React Scripts ke versi terbaru
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
